#:kivy 2.3.0


<ListScreen>:
    ScrollView:
        id: scroll
        pos_hint: {"center_x":0.5, "top":0.95}
        size_hint: .9,.75
        MDList:
            id: list_container

    MDIconButton:
        pos_hint: {"center_x":0.5, "center_y": 0.1}
        icon: "plus-thick"
        style: "filled"
        theme_bg_color: "Custom"
        md_bg_color: "#4169E1"
        theme_icon_color: "Custom"
        icon_color: "white"
        theme_font_size: "Custom"
        font_size: "32dp"
        radius: [self.height / 2, ]
        size_hint: None, None
        size: "48dp", "48dp"
        on_release : root.open_dialog()

<EditField>:
    id: edit_field
    on_text_validate: self.list_item.change_task_name(self.text)
    MDTextFieldHintText:
        text: "Enter new task name"


<ListItemBase>:
    MDListItemHeadlineText:
        id: list_headline_text
        text: root.headline_text
    MDListItemTrailingCheckbox:
        id: list_checkbox
        active: root.check
        on_release: root.mark(self.active)

<BaseDialogContent>:
    orientation: "vertical"
    MDDivider:
    MDTextField:
        id: name_field
        mode: "filled"
        on_focus: if self.focus: root.options_dropdown_menu_open()
        set_text: lambda instance, text: root.on_text_field_text()
        MDTextFieldHintText:
            text: "Enter name"
    MDDivider:

<OptionsListItem>:
    MDListItem:
        size_hint: 0.75, 1
        on_release: root.dialog.choose_name(root.headline_text)
        MDListItemHeadlineText:
            text: root.headline_text
    MDIconButton:
        pos_hint: {"x":0.75}
        size_hint: 0.25, 1
        icon: "trash-can-outline"
        style: "standard"
        on_release: root.dialog.delete_from_options(root.headline_text)
