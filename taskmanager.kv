#:kivy 2.3.0

#:import Menu modules.menu.menu.Menu
#:import Navigation modules.top_bar.top_bar.Navigation
#:import Tasks modules.tasks.tasks.Tasks
#:import Goals modules.goals.goals.Goals
#:import Calendar modules.calendar.calendar.Calendar
#:import Notes modules.notes.notes.Notes
#:import Stats modules.stats.stats.Stats
#:import Timer modules.timer.timer.Timer
#:import WeeklyTasks modules.weekly_tasks.weekly_tasks.WeeklyTasks

# Screen Manager for my app
MainScreen:

<MainScreen>:
    RelativeLayout:
        id: main_layout
        Navigation:
            id: nav
            pos_hint: {"x":0, "y":0.86}
            size_hint: 1, 0.15

        # Main Screen Manager for my mobile app
        MainScreenManager:
            id: sm
            pos_hint: {"x":0, "y":0}
            size_hint: 1, 0.86
            Menu:
            Tasks:
            Goals:
            Timer:
            WeeklyTasks:
            Notes:
            Stats:
            Calendar:
